import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'tarot-mobile-app',
  webDir: 'dist/tarot-mobile-app',
  bundledWebRuntime: false
};

export default config;
